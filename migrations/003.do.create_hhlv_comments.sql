CREATE TABLE hhlv_comments (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  text TEXT NOT NULL,
  date_created TIMESTAMP DEFAULT now() NOT NULL,
  deal_id INTEGER
    REFERENCES hhlv_deals(id) ON DELETE CASCADE NOT NULL,
  user_id INTEGER
    REFERENCES hhlv_users(id) ON DELETE CASCADE NOT NULL
);